# Optimized Embedding Service Requirements
fastapi==0.104.1
uvicorn[standard]==0.24.0
pydantic==2.5.0

# Core ML libraries (let sentence-transformers manage dependencies)
sentence-transformers==2.3.1

# Explicitly specify lighter versions if needed
numpy>=1.24.0,<2.0.0
torch>=2.0.0
